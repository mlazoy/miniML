inr inl (fun ( f181 : ((() -> int)
                       +
                       (()
                        ->
                        (bool + ()) * (() -> bool)
                        *
                        (int + ()))
                       ->
                       bool
                       *
                       ((bool
                         +
                         (bool + int * int))
                        *
                        ()))
                      +
                      bool ) ->
  false)

// // TSum TInt (TSum (TArrow (TSum (TArrow (TSum (TArrow TUnit TInt) (TArrow TUnit (TProd (TProd (TSum TBool TUnit) (TArrow TUnit TBool)) (TSum TInt TUnit)))) (TProd TBool (TProd (TSum TBool (TSum TBool (TProd TInt TInt))) TUnit))) TBool) TBool) TBool)