(fun ( y179 : ((int -> () * int)
               ->
               int
               +
               (()
                +
                (bool + bool * bool
                 +
                 ((int -> bool * ())
                  +
                  (() * () + (bool + bool))))
                ->
                ((bool -> ()) + bool * () + bool
                 +
                 (int + int))
                *
                (int
                 +
                 ((int -> () -> ()) -> int)))
               *
               bool)
              +
              ((int -> int -> bool * bool)
               ->
               bool) ) ->
      (fun ( lala12 : bool ) ->
          fun ( g58 : bool
                      *
                      (int
                       +
                       (bool
                        ->
                        bool
                        ->
                        (bool -> int) * bool
                        ->
                        bool + bool))
                      *
                      (int
                       +
                       (((int -> bool)
                         *
                         (bool * int)
                         *
                         bool
                         ->
                         ())
                        ->
                        ())
                       ->
                       ())
                      +
                      ()
                      ->
                      ((()
                        ->
                        ((bool * int -> bool * ())
                         ->
                         ())
                        ->
                        bool
                        ->
                        (bool + ())
                        *
                        (int + bool))
                       ->
                       int)
                      +
                      (bool
                       *
                       (bool + bool -> bool)
                       +
                       (() -> bool))
                      *
                      (() + (int -> int) -> ())
                      *
                      ((((bool -> bool)
                         ->
                         int * int)
                        +
                        int
                        ->
                        int)
                       +
                       ())
                      +
                      ((int -> int * int)
                       +
                       (bool
                        +
                        (int
                         ->
                         (() * int + bool)
                         *
                         (bool * bool
                          *
                          (() * int)))))
                      *
                      (int
                       +
                       ((int * int * (bool -> int)
                         *
                         int
                         ->
                         int
                         *
                         (() * (bool -> ())))
                        +
                        ((int * int -> ())
                         +
                         (bool + int * bool))
                        *
                        (() + (() + ()) -> bool)))
                      +
                      () ) ->
            23
            ::
            -12
            ::
            -4 :: 4 :: 7 :: 20 :: 14 :: 16 :: [])
        false)
    (let y108 : int = -19
    in
    inl (fun ( y153 : int -> () * int ) ->
      inl -2))
  ((let test_4218 : () = ()
    in
    fun ( y200 : int ) ->
      fun ( z185 : bool
                   *
                   (int
                    +
                    (bool
                     ->
                     bool
                     ->
                     (bool -> int) * bool
                     ->
                     bool + bool))
                   *
                   (int
                    +
                    (((int -> bool) * (bool * int)
                      *
                      bool
                      ->
                      ())
                     ->
                     ())
                    ->
                    ())
                   +
                   () ) ->
        inr ())
    (if false then 0 else 12))