let g1 : int =
  case [] of | [] -> -1 | g1 :: f2 -> 2
in
let rec x3 (x2 : bool) : int
                         *
                         (bool
                          +
                          (int
                           +
                           (() + ()
                            +
                            (bool -> bool)
                            +
                            (bool -> bool) * ())
                           *
                           (bool
                            ->
                            bool
                            +
                            (bool -> int))))
                         +
                         bool
                         +
                         (((int + int -> int)
                           +
                           (bool * (bool + bool)
                            ->
                            int)
                           *
                           int
                           ->
                           int
                           ->
                           bool
                           +
                           (bool + (() + ()))
                           +
                           bool)
                          *
                          int
                          ->
                          ((bool -> int * int)
                           *
                           (bool -> bool)
                           *
                           ((() + int
                             +
                             (int + int)
                             +
                             bool * int * bool)
                            *
                            int)
                           +
                           (bool * (int + int)
                            *
                            (() + ())
                            +
                            (bool
                             +
                             ((bool -> ()) + ()))
                            +
                            bool))
                          *
                          ((int
                            ->
                            ((() -> ()) * bool
                             ->
                             int * (() -> int))
                            ->
                            bool)
                           *
                           ()))
                         ->
                         int =
  fun ( x4 : int
             *
             (bool
              +
              (int
               +
               (() + () + (bool -> bool)
                +
                (bool -> bool) * ())
               *
               (bool -> bool + (bool -> int))))
             +
             bool
             +
             (((int + int -> int)
               +
               (bool * (bool + bool) -> int) * int
               ->
               int
               ->
               bool + (bool + (() + ())) + bool)
              *
              int
              ->
              ((bool -> int * int)
               *
               (bool -> bool)
               *
               ((() + int + (int + int)
                 +
                 bool * int * bool)
                *
                int)
               +
               (bool * (int + int) * (() + ())
                +
                (bool + ((bool -> ()) + ()))
                +
                bool))
              *
              ((int
                ->
                ((() -> ()) * bool
                 ->
                 int * (() -> int))
                ->
                bool)
               *
               ())) ) ->
    -3
in
fun ( test_423 : int ) -> ()